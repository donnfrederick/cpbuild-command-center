# PR Audit — feat/devtools-error-wrap-up

Pre-merge audit of pending changes.

---

## Issues to Fix Before PR

### 1. **public/sw.js — Consider excluding from PR**

`public/sw.js` is auto-generated by the PWA build (next-pwa). The diff shows updated chunk hashes from a local build. This file will be regenerated by CI on every build.

**Recommendation:** Revert `public/sw.js` before committing so the PR doesn’t include build artifacts. CI will regenerate it.

```bash
git checkout -- public/sw.js
```

### 2. **docs/ERROR_WRAPUP_FIXES.md — Misleading .env reference**

Current text: "UNIFIER_MOCK=true is set in .env and .env.example"

`.env` is gitignored and not committed. The doc should refer only to `.env.example` (or “copy to .env”).

**Fix:** Change to: "UNIFIER_MOCK=true is set in .env.example. Copy to .env when setting up."

### 3. **DEV_NOTES.md — Incorrect sw.js note**

Line 426 says: "Service worker: generated at build time into public/sw.js (gitignored)"

`public/sw.js` is not in `.gitignore` and is committed. The note is wrong.

**Fix:** Change to: "Service worker: generated at build time into public/sw.js"

### 4. **.env.example — NEXTAUTH_URL default**

Default is `http://localhost:3001` but the app runs on port 3002 (`npm run dev --port 3002`). The comment mentions both ports; the default may confuse users.

**Recommendation:** Either set default to `http://localhost:3002` or keep 3001 if Docker/other setups use 3001. Current comment is acceptable; no change required unless you want consistency.

---

## No Changes Needed

| Item | Status |
|------|--------|
| Unifier mock in production | Correctly blocked — only allowed when NODE_ENV !== production OR APP_ENV/RAILWAY is dev-like |
| unifier-test auth bypass | Correct — only in NODE_ENV !== production |
| DevTools ctx dependency fix | Correct — avoids infinite loop |
| ErrorWrapUp auto-run | Intentional — runs once on mount |
| Mock data shape | Matches UnifierProject interface |
| Tests | All pass (unit + integration) |

---

## Summary

All fixes applied. PR is ready.
