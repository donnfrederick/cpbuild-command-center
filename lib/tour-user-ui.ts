/**
 * End-user tour UI (TopBar picker, auto-launch, deploy/deep-link tours, notification CTAs).
 *
 * Set to `true` when production tours are actively maintained again.
 * DevTools → Site Tour Inspector is unaffected — it drives TourPlayer directly for dev QA.
 */
export const TOUR_USER_UI_ENABLED = false;
