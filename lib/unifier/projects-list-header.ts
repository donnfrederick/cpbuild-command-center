/**
 * GET /api/unifier/projects includes this header with the number of Field Tracker
 * projects that already have a Unifier PID (excluded from the JSON body).
 */
export const CC_UNIFIER_LINKED_COUNT_HEADER = "X-CC-Unifier-Linked-Count";
