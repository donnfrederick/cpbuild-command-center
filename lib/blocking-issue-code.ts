/** Stable JSON `code` when install complete is blocked (FB-0027). Safe to import from client bundles. */
export const BLOCKING_ISSUE_OPEN_CODE = "BLOCKING_ISSUE_OPEN" as const;

/** Stable JSON `code` when Install Complete-Verified is blocked — no subcontractor assigned. */
export const INSTALL_COMPLETE_NO_SUBCONTRACTOR_CODE =
  "INSTALL_COMPLETE_NO_SUBCONTRACTOR" as const;
