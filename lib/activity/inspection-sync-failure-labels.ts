/** Canonical English label stored in activity metadata when the form template has no name. */
export const UNKNOWN_INSPECTION_FORM_NAME = "Unknown form";

/** Fallback when sync error history has no message (should not happen for valid upserts). */
export const INSPECTION_SYNC_FAILED_DEFAULT_MESSAGE = "Inspection sync failed.";
