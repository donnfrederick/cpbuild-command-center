/** Max locations per PDF export request (narrow filters if exceeded). */
export const MEDIA_ALBUM_PDF_MAX_LOCATIONS = 80;

/** Max individual photos/videos embedded across all locations. */
export const MEDIA_ALBUM_PDF_MAX_ITEMS = 400;

/** Parallel album fetches per export batch. */
export const MEDIA_ALBUM_PDF_FETCH_CONCURRENCY = 6;
